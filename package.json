{
  "name": "zenoh",
  "type": "module",
  "exports": {
    "require": "./cjs/index.js",
    "import": "./esm/index.js"
  },
  "//types": ".//index.d.ts",
  "files": [
    "package.json",
    "README.md",
    "esm",
    "cjs",
    "LICENSE",
    "src"
  ],
  "dependencies": {
    "cpx": "^1.5.0",
    "text-encoding": "^0.7.0"
  },
  "devDependencies": {
    "@types/node": "20.9.1",
    "cpx": "^1.5.0",
    "exports-loader": "^4.0.0",
    "ts-loader": "^9.5.0",
    "ts-node": "10.9.1",
    "typescript": "^5.3.2",
    "webpack-cli": "^5.1.4"
  },
  "scripts": {
    "//   DEV   //": "DEV",
    "//build:wasm:cjs": "npm run build:wasm && npm run build:cjs",
    "//build:wasm:esm": "npm run build:wasm && npm run build:esm",
    "//build:ts": "npm run build:esm",
    "//  CLEAN   //": "",
    "clean": "rm -rf dist esm cjs && rm -rf node_modules",
    "//  BUILD   //": "",
    "build": "npm run build:wasm && npm run build:ts && npm run build:copy_zenoh_js && npm run build:copy_wasm",
    "build:wasm": "bash zenoh-wasm/build.sh",
    "build:ts": "npm run build:esm",
    "//build:cjs": "npx tsc --module commonjs --outDir cjs/ && echo '{\"type\": \"commonjs\"}' > cjs/package.json",
    "build:esm": "npx tsc --module es2022 --outDir esm/ && echo '{\"type\": \"module\"}' > esm/package.json",
    "build:copy_zenoh_js": "cpx -v \"src/wasm/**/*.{wasm,js,mjs,}\" \"esm/wasm\" && cpx -v \"src/wasm/**/*.{wasm,js,mjs,}\" \"cjs/wasm\"",
    "build:copy_wasm": "cpx -v \"src/**/*.wasm\" \"./cjs\" && cpx -v \"src/**/*.wasm\" \"./esm\"",
    "//   PACKAGE   //": "",
    "webpack": "webpack",
    "example": "cd examples && npm run start",
    "clean_build_package": "npm run clean && npm install && npm run build "
  }
}